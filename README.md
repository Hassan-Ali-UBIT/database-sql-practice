# database-sql-practice

This project just uses sqlite3 command line util for running sql against sqlite3 database.

install on linux via

```
sudo apt update && apt install -y sqlite3
```

sqlite3 < <path-to-sql-file>